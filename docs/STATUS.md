# Status — 2026-09-16

**Done and pushed (commit 9a32cc2):** pipeline 1→6, studio UI with blueprint + cover selection, design system (themes.json, editorial templates, font registry), publishing queue + 9 adapters (unverified on real accounts), auto-titles step, design brief (`docs/DESIGN-BRIEF.md`).

**Claude Design pass:** done by Gabriel on 2026-09-16 from the brief. The design output lives in Claude Design, not in this repo. Next step: export tokens/CSS + screen PNGs into `design/` and align `src/ui/index.html` and `templates/` with them.

**Next (in order):** 1) drop design exports into `design/` · 2) first real `queue.mjs login` + run on 小红书, fix locators · 3) replace placeholder SFX/bed with Gabriel's own materials via `materials/` · 4) domains + IPONZ search (`docs/BRAND.md`).
